/*
Deployment script for ReCaptDatabase

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "ReCaptDatabase"
:setvar DefaultFilePrefix "ReCaptDatabase"
:setvar DefaultDataPath "C:\Users\HP\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\HP\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Cars].[CategoryId] is being dropped, data loss could occur.

The type for column BrandId in table [dbo].[Cars] is currently  NVARCHAR (50) NOT NULL but is being changed to  INT NULL. Data loss could occur and deployment may fail if the column contains data that is incompatible with type  INT NULL.

The type for column ColorId in table [dbo].[Cars] is currently  NVARCHAR (50) NOT NULL but is being changed to  INT NULL. Data loss could occur and deployment may fail if the column contains data that is incompatible with type  INT NULL.

The type for column DailyPrice in table [dbo].[Cars] is currently  DECIMAL (18) NULL but is being changed to  MONEY NULL. Data loss could occur and deployment may fail if the column contains data that is incompatible with type  MONEY NULL.

The type for column Description in table [dbo].[Cars] is currently  NVARCHAR (70) NULL but is being changed to  NVARCHAR (50) NULL. Data loss could occur and deployment may fail if the column contains data that is incompatible with type  NVARCHAR (50) NULL.
*/

IF EXISTS (select top 1 1 from [dbo].[Cars])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[Cars]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Cars] (
    [CarId]       INT           IDENTITY (1, 1) NOT NULL,
    [BrandId]     INT           NULL,
    [ColorId]     INT           NULL,
    [ModelYear]   INT           NULL,
    [DailyPrice]  MONEY         NULL,
    [Description] NVARCHAR (50) NULL,
    PRIMARY KEY CLUSTERED ([CarId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Cars])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Cars] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Cars] ([CarId], [BrandId], [ColorId], [ModelYear], [DailyPrice], [Description])
        SELECT   [CarId],
                 [BrandId],
                 [ColorId],
                 [ModelYear],
                 [DailyPrice],
                 [Description]
        FROM     [dbo].[Cars]
        ORDER BY [CarId] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Cars] OFF;
    END

DROP TABLE [dbo].[Cars];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Cars]', N'Cars';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Update complete.';


GO
